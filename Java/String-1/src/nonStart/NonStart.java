package nonStart;

public class NonStart {
    //Retorne uma concatenação das duas Strings, porém sem as duas primeiras letras de cada uma. As Strings devem ter
    //um length de 1 ou mais.

    public String nonStart(String a, String b) {
        return a.substring(1)+b.substring(1);
    }
}
